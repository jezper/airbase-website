# Phase 1: Foundation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Set up the Next.js project with the complete design system (colors, typography, texture, motion), layout components (Nav, Footer, PageWrapper), dark/light theming, and a working home page skeleton that demonstrates the "high-energy editorial" visual direction.

**Architecture:** Next.js 14+ App Router with TypeScript and Tailwind CSS. CSS custom properties for theming (dark-first). Google Fonts loaded via next/font. Grain texture overlay via CSS pseudo-element. Mobile-first responsive design. The foundation produces a deployable site with navigation, footer, theme toggle, and a static hero section.

**Tech Stack:** Next.js 14+, TypeScript, Tailwind CSS 3.x, next-themes, lucide-react (icons), Google Fonts (Fraunces, Syne, JetBrains Mono)

**Reference docs:**
- `docs/superpowers/specs/2026-04-12-visual-direction-design.md` (approved visual direction)
- `docs/SPEC.md` (project specification)
- `docs/DESIGN-TOKENS.md` (starting tokens, superseded by visual direction spec)
- `content/Airbase Logo – Black.svg` (actual logo SVG)

---

## File Structure

```
airbase-website/
├── src/
│   ├── app/
│   │   ├── layout.tsx          # Root layout: fonts, theme provider, grain overlay
│   │   ├── page.tsx            # Home page: hero + stats bar + feed placeholder
│   │   ├── globals.css         # CSS custom properties, base styles, grain, utilities
│   │   └── favicon.ico
│   ├── components/
│   │   ├── nav.tsx             # Top navigation bar
│   │   ├── mobile-menu.tsx     # Full-screen mobile menu overlay
│   │   ├── footer.tsx          # Footer with social links, label scroller, newsletter
│   │   ├── theme-toggle.tsx    # Dark/light mode toggle button
│   │   ├── logo.tsx            # Airbase SVG wordmark component
│   │   └── page-wrapper.tsx    # Content wrapper with max-width and padding
│   └── lib/
│       └── fonts.ts            # Google Font declarations (Fraunces, Syne, JetBrains Mono)
├── public/
│   └── airbase-logo.svg        # (already exists)
├── tailwind.config.ts          # Custom theme: colors, fonts, spacing
├── tsconfig.json               # (generated by create-next-app)
├── next.config.ts              # Next.js configuration
└── package.json                # (generated by create-next-app)
```

---

### Task 1: Initialize Next.js Project

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.ts`, `tailwind.config.ts`, `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/globals.css`

- [ ] **Step 1: Create the Next.js project**

Run from the project root. The `--use-npm` flag keeps things simple. Since files already exist in the directory, we use `--no-git` and initialize manually.

```bash
cd /Users/jezper.lorne/Projects/Code/airbase-website
npx create-next-app@latest . --typescript --tailwind --app --src-dir --no-git --use-npm --import-alias "@/*"
```

When prompted, accept defaults (yes to ESLint, no to Turbopack unless prompted).

- [ ] **Step 2: Install additional dependencies**

```bash
npm install next-themes lucide-react
```

- [ ] **Step 3: Initialize git and make initial commit**

```bash
git init
git add -A
git commit -m "chore: initialize Next.js 14 project with TypeScript and Tailwind"
```

- [ ] **Step 4: Verify dev server starts**

```bash
npm run dev
```

Expected: Server starts on http://localhost:3000, default Next.js page renders.

Stop the server (Ctrl+C) after verifying.

---

### Task 2: Configure Fonts

**Files:**
- Create: `src/lib/fonts.ts`
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Create the font declarations**

Create `src/lib/fonts.ts`:

```typescript
import { Fraunces, Syne, JetBrains_Mono } from "next/font/google";

export const fraunces = Fraunces({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-display",
  axes: ["opsz"],
});

export const syne = Syne({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-body",
  weight: ["400", "500", "600", "700", "800"],
});

export const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-mono",
  weight: ["400", "500", "600"],
});
```

- [ ] **Step 2: Wire fonts into root layout**

Replace `src/app/layout.tsx`:

```typescript
import type { Metadata } from "next";
import { fraunces, syne, jetbrainsMono } from "@/lib/fonts";
import "./globals.css";

export const metadata: Metadata = {
  title: "Airbase",
  description: "Airbase — Swedish trance producer and DJ",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${fraunces.variable} ${syne.variable} ${jetbrainsMono.variable}`}
      suppressHydrationWarning
    >
      <body className="font-body bg-bg text-text antialiased">
        {children}
      </body>
    </html>
  );
}
```

- [ ] **Step 3: Verify fonts load**

Update `src/app/page.tsx` temporarily:

```typescript
export default function Home() {
  return (
    <main className="p-12">
      <h1 className="font-display text-6xl font-black">Everything Else Could Wait</h1>
      <p className="font-body text-lg mt-4">Body text in Syne — angular, assertive.</p>
      <p className="font-mono text-sm mt-2">2026-02-06 · Black Hole Recordings</p>
    </main>
  );
}
```

Run `npm run dev` and verify three distinct fonts render.

- [ ] **Step 4: Commit**

```bash
git add src/lib/fonts.ts src/app/layout.tsx src/app/page.tsx
git commit -m "feat: configure Google Fonts — Fraunces, Syne, JetBrains Mono"
```

---

### Task 3: Design System — Tailwind Config and CSS Custom Properties

**Files:**
- Modify: `tailwind.config.ts`
- Modify: `src/app/globals.css`

- [ ] **Step 1: Configure Tailwind with the full design system**

Replace `tailwind.config.ts`:

```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./src/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  darkMode: "class",
  theme: {
    extend: {
      fontFamily: {
        display: ["var(--font-display)", "Georgia", "serif"],
        body: ["var(--font-body)", "Helvetica Neue", "sans-serif"],
        mono: ["var(--font-mono)", "Menlo", "monospace"],
      },
      colors: {
        bg: "var(--color-bg)",
        "bg-card": "var(--color-bg-card)",
        "bg-elevated": "var(--color-bg-elevated)",
        text: "var(--color-text)",
        "text-muted": "var(--color-text-muted)",
        "text-faint": "var(--color-text-faint)",
        accent: "var(--color-accent)",
        "accent-hover": "var(--color-accent-hover)",
        gold: "var(--color-gold)",
      },
      borderColor: {
        DEFAULT: "var(--color-border)",
        hover: "var(--color-border-hover)",
        section: "var(--color-border-section)",
      },
      maxWidth: {
        content: "1200px",
        prose: "780px",
        "prose-narrow": "680px",
      },
      fontSize: {
        hero: "clamp(3.5rem, 10vw, 8.75rem)",
      },
      lineHeight: {
        hero: "0.88",
      },
      letterSpacing: {
        hero: "-0.02em",
      },
      animation: {
        "glow-breathe": "glow-breathe 6s ease-in-out infinite alternate",
      },
      keyframes: {
        "glow-breathe": {
          "0%": { opacity: "0.7", transform: "scale(1)" },
          "100%": { opacity: "1", transform: "scale(1.05)" },
        },
      },
    },
  },
  plugins: [],
};

export default config;
```

- [ ] **Step 2: Set up CSS custom properties and base styles**

Replace `src/app/globals.css`:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  /* ── Dark Theme (Primary / Default) ── */
  :root {
    --color-bg: #0C0B0A;
    --color-bg-card: #141210;
    --color-bg-elevated: #1A1816;
    --color-text: #EDE7DF;
    --color-text-muted: #9A928A;
    --color-text-faint: #5A5550;
    --color-accent: #E85D26;
    --color-accent-hover: #F06D36;
    --color-accent-glow: rgba(232, 93, 38, 0.12);
    --color-gold: #C4A87C;
    --color-border: rgba(237, 231, 223, 0.04);
    --color-border-hover: rgba(237, 231, 223, 0.10);
    --color-border-section: rgba(237, 231, 223, 0.06);
  }

  /* ── Light Theme ── */
  .light {
    --color-bg: #F4EEE8;
    --color-bg-card: #EAE3D9;
    --color-bg-elevated: #FDFBF8;
    --color-text: #1A1816;
    --color-text-muted: #78706A;
    --color-text-faint: #A49B93;
    --color-accent: #D4551E;
    --color-accent-hover: #BF4A18;
    --color-accent-glow: rgba(212, 85, 30, 0.08);
    --color-gold: #B09468;
    --color-border: rgba(26, 24, 22, 0.08);
    --color-border-hover: rgba(26, 24, 22, 0.14);
    --color-border-section: rgba(26, 24, 22, 0.06);
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    min-height: 100dvh;
    position: relative;
  }

  /* ── Grain Texture Overlay ── */
  body::after {
    content: "";
    position: fixed;
    inset: 0;
    opacity: 0.045;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    background-size: 200px;
    pointer-events: none;
    z-index: 9999;
  }

  /* ── Selection Color ── */
  ::selection {
    background: var(--color-accent);
    color: var(--color-bg);
  }
}

@layer components {
  /* ── Gradient Accent Line ── */
  .accent-line {
    height: 3px;
    background: linear-gradient(to right, var(--color-accent), transparent 70%);
    border-radius: 2px;
  }
}

@layer utilities {
  /* ── Reduced Motion ── */
  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
  }
}
```

- [ ] **Step 3: Verify the design system renders**

Update `src/app/page.tsx`:

```typescript
export default function Home() {
  return (
    <main className="min-h-screen bg-bg text-text p-12">
      <h1 className="font-display text-hero font-black leading-hero tracking-hero">
        Everything<br />Else Could<br />Wait
      </h1>
      <p className="font-body text-lg text-text-muted mt-6 max-w-prose">
        Body text in Syne. Angular, assertive, art-world energy.
      </p>
      <p className="font-mono text-sm text-text-faint mt-2">
        2026-02-06 · Single · Black Hole Recordings
      </p>
      <button className="mt-6 bg-accent text-bg font-body text-sm font-bold uppercase tracking-widest px-6 py-3 rounded">
        Listen Now
      </button>
      <div className="accent-line mt-12 mx-0" />
      <div className="mt-8 bg-bg-card border border-DEFAULT rounded-lg p-6">
        <p className="text-gold font-mono text-xs uppercase tracking-wider">Card on bg-card</p>
      </div>
    </main>
  );
}
```

Run `npm run dev`. Verify: dark background (#0C0B0A), warm off-white text, fire orange button, grain overlay, Fraunces hero title, accent gradient line.

- [ ] **Step 4: Commit**

```bash
git add tailwind.config.ts src/app/globals.css src/app/page.tsx
git commit -m "feat: design system — colors, typography scale, grain texture, CSS custom properties"
```

---

### Task 4: Theme Provider (Dark/Light Mode)

**Files:**
- Create: `src/components/theme-toggle.tsx`
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Create the theme toggle component**

Create `src/components/theme-toggle.tsx`:

```typescript
"use client";

import { useTheme } from "next-themes";
import { useEffect, useState } from "react";
import { Sun, Moon } from "lucide-react";

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();
  const [mounted, setMounted] = useState(false);

  useEffect(() => setMounted(true), []);

  if (!mounted) {
    return <button className="w-10 h-10" aria-label="Toggle theme" />;
  }

  const isDark = theme === "dark";

  return (
    <button
      onClick={() => setTheme(isDark ? "light" : "dark")}
      className="w-10 h-10 flex items-center justify-center rounded-lg text-text-faint hover:text-text transition-colors duration-150"
      aria-label={isDark ? "Switch to light mode" : "Switch to dark mode"}
    >
      {isDark ? <Sun size={18} /> : <Moon size={18} />}
    </button>
  );
}
```

- [ ] **Step 2: Wrap the app in ThemeProvider**

Update `src/app/layout.tsx`:

```typescript
import type { Metadata } from "next";
import { ThemeProvider } from "next-themes";
import { fraunces, syne, jetbrainsMono } from "@/lib/fonts";
import "./globals.css";

export const metadata: Metadata = {
  title: "Airbase",
  description: "Airbase — Swedish trance producer and DJ",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${fraunces.variable} ${syne.variable} ${jetbrainsMono.variable}`}
      suppressHydrationWarning
    >
      <body className="font-body bg-bg text-text antialiased">
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange={false}
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 3: Add toggle to test page and verify**

Update `src/app/page.tsx` to include the toggle temporarily:

```typescript
import { ThemeToggle } from "@/components/theme-toggle";

export default function Home() {
  return (
    <main className="min-h-screen bg-bg text-text p-12">
      <div className="flex justify-end mb-8">
        <ThemeToggle />
      </div>
      <h1 className="font-display text-hero font-black leading-hero tracking-hero">
        Everything<br />Else Could<br />Wait
      </h1>
      <p className="font-body text-lg text-text-muted mt-6 max-w-prose">
        Body text in Syne. Toggle the theme to see both modes.
      </p>
      <button className="mt-6 bg-accent text-bg font-body text-sm font-bold uppercase tracking-widest px-6 py-3 rounded">
        Listen Now
      </button>
      <div className="accent-line mt-12" />
    </main>
  );
}
```

Run `npm run dev`. Click the toggle. Verify: dark mode uses #0C0B0A bg with #E85D26 accent. Light mode uses #F4EEE8 bg with #D4551E accent. Grain overlay visible in both. Transition is smooth.

- [ ] **Step 4: Commit**

```bash
git add src/components/theme-toggle.tsx src/app/layout.tsx src/app/page.tsx
git commit -m "feat: dark/light theme toggle with next-themes, dark-first default"
```

---

### Task 5: Logo Component

**Files:**
- Create: `src/components/logo.tsx`

- [ ] **Step 1: Create the logo component from the actual SVG**

The logo SVG is in `content/Airbase Logo – Black.svg`. We inline it as a React component so the fill color adapts to the theme via `currentColor`.

Create `src/components/logo.tsx`:

```typescript
export function Logo({ className = "" }: { className?: string }) {
  return (
    <svg
      viewBox="0 0
92.4501 66.46 88.5301C65.98 87.1801 65.37 86.5801 63.82 86.6001C54.58 86.6801 45.33 86.6001 36.09 86.7001C35.26 86.7001 33.93 87.3701 33.68 88.0501C29.1 100.45 24.65 112.91 20.19 125.35C20.09 125.64 20.11 125.97 20.02 126.69H37.63C37.63 127.01 37.63 127.32 37.64 127.64L37.65 127.62ZM50.03 43.8301C49.83 43.8601 49.63 43.8801 49.44 43.9101C44.56 57.5501 39.69 71.1801 34.78 84.9101H65.09C60 71.0401 55.02 57.4301 50.03 43.8301Z" fill="currentColor"/>
      <path d="M431.04 126.69H448.93C447.38 128.31 413.01 128.46 410.61 126.83C411.92 126.69 412.73 126.53 413.55 126.53C417.8 126.5 422.05 126.41 426.29 126.56C428.27 126.63 429.15 126.04 429.84 124.1C439.73 96.29 449.72 68.51 459.7 40.73C464.33 27.82 468.98 14.91 473.63 2C473.82 1.46 474.06 0.93 474.44 0C475.13 1.75 475.72 3.16 476.25 4.58C490.1 41.21 504.11 77.79 517.68 114.53C520.54 122.28 525.59 125.57 533.27 126.12C534.28 126.19 535.27 126.55 536.2 127.04C533.73 128.46 488.92 128.2 487.07 126.51C493.1 126.51 498.5 126.51 503.9 126.51C509.3 126.51 514.89 126.51 519.55 126.51C514.52 125.05 508.62 123.74 503.02 121.62C492.61 117.67 485.25 110.47 481.61 99.72C480.33 95.94 478.8 92.25 477.48 88.48C477 87.1 476.3 86.57 474.77 86.58C465.53 86.66 456.29 86.58 447.04 86.68C446.23 86.68 444.94 87.42 444.68 88.12C440.13 100.44 435.7 112.81 431.26 125.18C431.15 125.47 431.16 125.81 431.03 126.69H431.04ZM476.13 85.01C471.02 71.07 466.02 57.43 460.75 43.07C455.59 57.5 450.7 71.15 445.75 85.01H476.13Z" fill="currentColor"/>
      <path d="M166.27 3.14012C156.34 3.14012 146.68 3.14012 137.02 3.12012C136.98 3.12012 136.93 2.83012 136.81 2.37012H183.72C183.14 2.89012 182.83 3.26012 182.44 3.47012C182.17 3.61012 181.79 3.54012 181.46 3.54012C172.54 3.39012 170.85 7.20012 170.86 14.9401C170.91 42.4401 170.98 69.9301 170.82 97.4301C170.75 109.35 165.13 118.06 154.28 123.19C152.26 124.14 150.1 124.8 148.01 125.6C147.5 125.79 147.01 126.02 146.57 126.63H176.18C176.23 126.93 176.29 127.24 176.34 127.54C175.49 127.69 174.64 127.97 173.79 127.97C160.55 128 147.31 127.99 134.07 127.99C133.07 127.99 132.07 128.04 131.07 127.97C130.57 127.93 130.08 127.68 129.58 127.53C129.58 127.31 129.58 127.09 129.58 126.87C130.3 126.74 131.01 126.56 131.74 126.49C140.38 125.75 142.19 123.84 142.2 115.09C142.23 88.5901 142.18 62.1001 142.25 35.6001C142.26 32.1301 142.43 28.5601 143.22 25.2001C145.77 14.3001 153.43 8.27012 163.55 4.80012C164.34 4.53012 165.13 4.28012 165.91 4.02012C165.96 4.00012 165.97 3.88012 166.28 3.16012L166.27 3.14012Z" fill="currentColor"/>
    </svg>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/logo.tsx
git commit -m "feat: Airbase logo component with currentColor fill for theme support"
```

---

### Task 6: Navigation Component

**Files:**
- Create: `src/components/nav.tsx`
- Create: `src/components/mobile-menu.tsx`

- [ ] **Step 1: Create the desktop navigation**

Create `src/components/nav.tsx`:

```typescript
"use client";

import { useState } from "react";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { Menu } from "lucide-react";
import { Logo } from "./logo";
import { ThemeToggle } from "./theme-toggle";
import { MobileMenu } from "./mobile-menu";

const NAV_LINKS = [
  { href: "/", label: "Feed" },
  { href: "/discography", label: "Discography" },
  { href: "/shows", label: "Shows" },
  { href: "/mixes", label: "Mixes" },
  { href: "/about", label: "About" },
  { href: "/contact", label: "Contact" },
];

export function Nav() {
  const pathname = usePathname();
  const [menuOpen, setMenuOpen] = useState(false);

  return (
    <>
      <nav className="flex items-center justify-between px-6 md:px-12 py-5 border-b border-section">
        <Link href="/" aria-label="Airbase home">
          <Logo className="h-5 text-text" />
        </Link>

        {/* Desktop links */}
        <div className="hidden md:flex items-center gap-7">
          {NAV_LINKS.map((link) => (
            <Link
              key={link.href}
              href={link.href}
              className={`font-body text-[13px] font-bold uppercase tracking-[0.12em] transition-colors duration-150 ${
                pathname === link.href
                  ? "text-text"
                  : "text-text-faint hover:text-text"
              }`}
            >
              {link.label}
            </Link>
          ))}
          <ThemeToggle />
        </div>

        {/* Mobile menu button */}
        <div className="flex items-center gap-2 md:hidden">
          <ThemeToggle />
          <button
            onClick={() => setMenuOpen(true)}
            className="w-10 h-10 flex items-center justify-center text-text-faint hover:text-text transition-colors"
            aria-label="Open menu"
          >
            <Menu size={22} />
          </button>
        </div>
      </nav>

      <MobileMenu
        open={menuOpen}
        onClose={() => setMenuOpen(false)}
        links={NAV_LINKS}
        pathname={pathname}
      />
    </>
  );
}
```

- [ ] **Step 2: Create the mobile menu overlay**

Create `src/components/mobile-menu.tsx`:

```typescript
"use client";

import { useEffect } from "react";
import Link from "next/link";
import { X } from "lucide-react";

interface MobileMenuProps {
  open: boolean;
  onClose: () => void;
  links: { href: string; label: string }[];
  pathname: string;
}

export function MobileMenu({ open, onClose, links, pathname }: MobileMenuProps) {
  useEffect(() => {
    if (open) {
      document.body.style.overflow = "hidden";
    } else {
      document.body.style.overflow = "";
    }
    return () => {
      document.body.style.overflow = "";
    };
  }, [open]);

  if (!open) return null;

  return (
    <div className="fixed inset-0 z-50 bg-bg flex flex-col">
      <div className="flex justify-end px-6 py-5">
        <button
          onClick={onClose}
          className="w-10 h-10 flex items-center justify-center text-text-faint hover:text-text transition-colors"
          aria-label="Close menu"
        >
          <X size={22} />
        </button>
      </div>

      <div className="flex-1 flex flex-col items-center justify-center gap-8">
        {links.map((link) => (
          <Link
            key={link.href}
            href={link.href}
            onClick={onClose}
            className={`font-body text-3xl font-bold uppercase tracking-[0.08em] transition-colors ${
              pathname === link.href
                ? "text-accent"
                : "text-text hover:text-accent"
            }`}
          >
            {link.label}
          </Link>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add src/components/nav.tsx src/components/mobile-menu.tsx
git commit -m "feat: navigation with mobile full-screen overlay"
```

---

### Task 7: Footer and PageWrapper Components

**Files:**
- Create: `src/components/footer.tsx`
- Create: `src/components/page-wrapper.tsx`

- [ ] **Step 1: Create the footer**

Create `src/components/footer.tsx`:

```typescript
import { Logo } from "./logo";

const SOCIAL_LINKS = [
  { label: "Spotify", href: "https://open.spotify.com/artist/0DgL6pCI4mC68FVMPlzx3L" },
  { label: "Apple Music", href: "https://music.apple.com/artist/airbase" },
  { label: "Beatport", href: "https://www.beatport.com/artist/airbase/8317" },
  { label: "SoundCloud", href: "https://soundcloud.com/airbasemusic" },
  { label: "YouTube", href: "https://youtube.com/@airbasemusic" },
  { label: "Instagram", href: "https://instagram.com/airbasemusic" },
  { label: "X", href: "https://x.com/airaborase" },
  { label: "Facebook", href: "https://facebook.com/airbasemusic" },
];

const LABELS = [
  "Black Hole", "Armada", "ASOT", "In Trance We Trust", "Platipus",
  "Flashover", "Anjunabeats", "High Contrast", "Magik Muzik",
  "Discover", "Mondo", "Moonrising", "Intuition",
];

export function Footer() {
  const labelText = [...LABELS, ...LABELS].join("  ·  ");

  return (
    <footer className="border-t border-section mt-16">
      {/* Label scroller */}
      <div className="overflow-hidden py-4 border-b border-section">
        <div className="animate-[scroll-left_40s_linear_infinite] whitespace-nowrap font-mono text-[11px] uppercase tracking-[0.1em] text-text-faint">
          {labelText}
        </div>
      </div>

      <div className="px-6 md:px-12 py-10 flex flex-col md:flex-row items-start md:items-center justify-between gap-8">
        {/* Logo + copyright */}
        <div>
          <Logo className="h-4 text-text-faint mb-3" />
          <p className="font-mono text-[11px] text-text-faint">
            &copy; {new Date().getFullYear()} Jezper S&ouml;derlund. All rights reserved.
          </p>
        </div>

        {/* Social links */}
        <div className="flex flex-wrap gap-4">
          {SOCIAL_LINKS.map((link) => (
            <a
              key={link.label}
              href={link.href}
              target="_blank"
              rel="noopener noreferrer"
              className="font-body text-xs font-semibold uppercase tracking-[0.08em] text-text-faint hover:text-accent transition-colors duration-150"
            >
              {link.label}
            </a>
          ))}
        </div>
      </div>
    </footer>
  );
}
```

- [ ] **Step 2: Add the label scroller keyframe to globals.css**

Add inside the existing `@layer utilities` block in `src/app/globals.css`, before the closing `}`:

```css
  @keyframes scroll-left {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
  }
```

- [ ] **Step 3: Create the page wrapper**

Create `src/components/page-wrapper.tsx`:

```typescript
export function PageWrapper({
  children,
  className = "",
}: {
  children: React.ReactNode;
  className?: string;
}) {
  return (
    <main className={`w-full max-w-content mx-auto px-6 md:px-12 ${className}`}>
      {children}
    </main>
  );
}
```

- [ ] **Step 4: Commit**

```bash
git add src/components/footer.tsx src/components/page-wrapper.tsx src/app/globals.css
git commit -m "feat: footer with label scroller and social links, page wrapper component"
```

---

### Task 8: Assemble Layout and Home Page Hero

**Files:**
- Modify: `src/app/layout.tsx`
- Modify: `src/app/page.tsx`

- [ ] **Step 1: Wire Nav and Footer into the root layout**

Update `src/app/layout.tsx`:

```typescript
import type { Metadata } from "next";
import { ThemeProvider } from "next-themes";
import { fraunces, syne, jetbrainsMono } from "@/lib/fonts";
import { Nav } from "@/components/nav";
import { Footer } from "@/components/footer";
import "./globals.css";

export const metadata: Metadata = {
  title: "Airbase",
  description: "Airbase — Swedish trance producer and DJ. 25 years of melody and restraint.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${fraunces.variable} ${syne.variable} ${jetbrainsMono.variable}`}
      suppressHydrationWarning
    >
      <body className="font-body bg-bg text-text antialiased">
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange={false}
        >
          <a
            href="#main-content"
            className="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[100] focus:px-4 focus:py-2 focus:bg-accent focus:text-bg focus:rounded font-body text-sm font-bold"
          >
            Skip to content
          </a>
          <Nav />
          <div id="main-content">
            {children}
          </div>
          <Footer />
        </ThemeProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 2: Build the home page with hero and stats bar**

Replace `src/app/page.tsx`:

```typescript
import { Play } from "lucide-react";

const STATS = [
  { number: "25+", label: "Years Active" },
  { number: "100+", label: "Releases" },
  { number: "14", label: "Aliases" },
  { number: "138", label: "BPM" },
];

const STREAM_LINKS = [
  { label: "Spotify", href: "https://open.spotify.com/album/4ZfQAvaurXtnLDajcazRHE" },
  { label: "Beatport", href: "https://www.beatport.com/track/everything-else-could-wait/23324813" },
  { label: "YouTube", href: "#" },
  { label: "Apple Music", href: "#" },
];

export default function Home() {
  return (
    <>
      {/* Hero */}
      <section className="relative min-h-[85vh] md:min-h-[85vh] flex flex-col justify-end px-6 md:px-12 pb-16 md:pb-20 overflow-hidden">
        {/* Ambient glow */}
        <div
          className="absolute -top-[30%] -right-[20%] w-[80%] h-[140%] rounded-full pointer-events-none blur-[60px] motion-safe:animate-glow-breathe"
          style={{ background: "radial-gradient(ellipse at center, var(--color-accent-glow) 0%, transparent 65%)" }}
          aria-hidden="true"
        />

        <div className="relative z-10">
          <p className="font-mono text-[13px] font-medium uppercase tracking-[0.2em] text-accent mb-4 md:mb-5">
            New Release / 2026
          </p>
          <h1 className="font-display text-hero font-black leading-hero tracking-hero mb-5 md:mb-6">
            Everything<br />Else Could<br />Wait
          </h1>
          <p className="font-body text-[15px] font-extrabold uppercase tracking-[0.15em] text-accent mb-8">
            Airbase &mdash; Black Hole Recordings
          </p>

          <div className="flex flex-col sm:flex-row items-start sm:items-center gap-5">
            <button
              className="w-16 h-16 rounded-full bg-accent flex items-center justify-center hover:scale-110 transition-transform duration-150 flex-shrink-0"
              aria-label="Play Everything Else Could Wait"
            >
              <Play size={22} fill="#0C0B0A" stroke="#0C0B0A" className="ml-1" />
            </button>
            <div>
              <div className="flex flex-wrap gap-x-5 gap-y-2">
                {STREAM_LINKS.map((link) => (
                  <a
                    key={link.label}
                    href={link.href}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="font-body text-[13px] font-bold uppercase tracking-[0.08em] text-accent hover:border-b-2 hover:border-accent pb-0.5 transition-[border-color] duration-150"
                  >
                    {link.label}
                  </a>
                ))}
              </div>
              <p className="font-mono text-xs text-text-faint mt-2">
                Single &mdash; 07:23 &mdash; 138 BPM
              </p>
            </div>
          </div>
        </div>
      </section>

      {/* Accent line */}
      <div className="accent-line mx-6 md:mx-12" />

      {/* Stats bar */}
      <section className="flex flex-wrap justify-between px-6 md:px-12 py-8 border-b border-section">
        {STATS.map((stat) => (
          <div key={stat.label} className="text-center px-4 py-3 flex-1 min-w-[120px]">
            <div className="font-display text-5xl font-black text-accent" style={{ fontVariationSettings: "'opsz' 144" }}>
              {stat.number}
            </div>
            <div className="font-mono text-[10px] uppercase tracking-[0.15em] text-text-faint mt-1">
              {stat.label}
            </div>
          </div>
        ))}
      </section>

      {/* Feed placeholder */}
      <section className="px-6 md:px-12 py-12">
        <div className="flex items-center justify-between mb-8">
          <h2 className="font-body text-[13px] font-bold uppercase tracking-[0.12em] text-text-faint">
            Feed
          </h2>
        </div>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
          {[1, 2, 3, 4].map((i) => (
            <div
              key={i}
              className="bg-bg-card rounded-lg border border-DEFAULT p-8 hover:border-hover hover:-translate-y-1 transition-all duration-150"
            >
              <p className="font-mono text-xs text-text-faint uppercase tracking-wider">
                Feed card placeholder {i}
              </p>
            </div>
          ))}
        </div>
      </section>
    </>
  );
}
```

- [ ] **Step 3: Verify the full page**

Run `npm run dev`. Check:
- Nav renders with logo, links, theme toggle
- Hero: massive Fraunces title, fire orange accent, ambient glow animation, play button, streaming links
- Stats bar with bold numbers
- Feed placeholder cards with hover effect
- Footer with label scroller and social links
- Grain overlay visible across everything
- Theme toggle switches between dark and light
- Mobile (resize to 320px): hamburger menu, stacked layout, hero still bold

- [ ] **Step 4: Run the build**

```bash
npm run build
```

Expected: Build succeeds with no errors.

- [ ] **Step 5: Commit**

```bash
git add src/app/layout.tsx src/app/page.tsx
git commit -m "feat: complete Phase 1 foundation — hero, stats, nav, footer, theming"
```

---

## Verification Checklist

After completing all tasks, verify:

- [ ] `npm run build` passes with no errors
- [ ] `npm run dev` serves locally
- [ ] Dark theme renders: #0C0B0A bg, #E85D26 accent, #EDE7DF text
- [ ] Light theme renders: #F4EEE8 bg, #D4551E accent, #1A1816 text
- [ ] Hero title is massive (clamp 64-140px) in Fraunces Black
- [ ] Syne used for all body/UI text
- [ ] JetBrains Mono used for dates and metadata
- [ ] Grain texture visible in both themes
- [ ] Ambient glow animates behind hero
- [ ] Navigation works (logo, links, theme toggle)
- [ ] Mobile menu opens/closes (full-screen overlay)
- [ ] Footer shows social links and label scroller
- [ ] Skip-to-content link works on keyboard focus
- [ ] Page is responsive at 320px, 768px, 1024px, 1440px
- [ ] No console errors
